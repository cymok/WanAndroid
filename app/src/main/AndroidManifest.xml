<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Android 11+ -->
    <queries>
        <package android:name="com.tencent.mm" />
    </queries>

    <!-- 适配 Android 10 沙盒
    android:requestLegacyExternalStorage="true"
    -->
    <!-- WebView 支持明文流量 即 Http
    android:usesCleartextTraffic="true"
    -->
    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:replace="android:name, android:allowBackup, android:icon, android:label, android:roundIcon, android:theme"
        tools:targetApi="33">

        <activity
            android:name=".index.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".index.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".index.setting.SettingActivity"
            android:exported="false"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".index.home.HomeActivity"
            android:exported="false" />
<!--
        <activity
            android:name=".index.home.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
-->
        <activity
            android:name=".index.web.WebActivity"
            android:exported="false"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".index.article.ArticleListActivity"
            android:exported="false"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".index.like.ArticleLikeActivity"
            android:exported="false"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".index.home.project.ProjectActivity"
            android:exported="false"
            android:theme="@style/AppTheme.ActionBar" />

        <activity
            android:name=".others.main.MainActivity"
            android:exported="false" />
        <activity
            android:name=".others.first.FirstActivity"
            android:exported="false" />
        <activity
            android:name=".others.second.SecondActivity"
            android:exported="false" />
        <activity
            android:name=".others.third.ThirdActivity"
            android:exported="false" />
        <activity
            android:name=".others.coroutine.CoroutineActivity"
            android:exported="false" />
        <activity
            android:name=".others.compose.ComposeActivity"
            android:exported="false" />

        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:exported="false" />

        <!--wechat start-->
        <activity
            android:name=".wxapi.WXEntryActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity="${applicationId}"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity
            android:name=".wxapi.WXPayEntryActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity="${applicationId}"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <!--wechat end-->

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileProvider"
            android:enabled="true"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!--适配华为（huawei）刘海屏-->
        <meta-data
            android:name="android.notch_support"
            android:value="true" />
        <!--适配小米（xiaomi）刘海屏-->
        <meta-data
            android:name="notch.config"
            android:value="portrait|landscape" />

    </application>

</manifest>